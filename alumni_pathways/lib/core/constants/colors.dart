import 'package:flutter/material.dart';

class TAppColors {
  static const Color primary = Color(0xFF088C45); // 60% - Dominant (Green)
  static const Color secondary = Color(
    0xFFFFD700,
  ); // 30% - Secondary (Gold/Yellow)

  // Additional Colors
  static const Color success = Color(
    0xFF4CAF50,
  ); // Green (for positive actions)
  static const Color info = Color(0xFF2196F3); // Blue (for informational UI)

  // Separate accent colors for light and dark mode
  static const Color lightAccent = Color(
    0xFF1E1E1E,
  ); // Dark Grey for Light Mode
  static const Color darkAccent = Color(0xFFFFFFFF); // White for Dark Mode
}
